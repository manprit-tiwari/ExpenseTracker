import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.expensetracker.app',
  appName: 'ExpenseTracker',
  webDir: 'dist/expense-tracker',
  bundledWebRuntime: false
};

export default config;
